# Nest
nest:
  client_id: !secret nest_client_id
  client_secret: !secret nest_client_secret

# Dyson
dyson:
  username: !secret email
  password: !secret dyson_password
  language: US
  devices:
    - device_id: PE7-US-HGA0460A
      device_ip: !secret dyson_ip
